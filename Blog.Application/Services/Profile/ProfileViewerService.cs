using Blog.Domain.Entities.User;
using Blog.Domain.Entities.User.Vo;

namespace Blog.Application.Services.Profile;

public sealed class ProfileViewerService
{
    public UserEntity ViewUser(UserId issuerId, UserId userId)
    {
        throw new NotImplementedException();
    }

    public UserEntity GetUserProfile(UserId issuerId, UserId userId)
    {
        throw new NotImplementedException();
    }
}
